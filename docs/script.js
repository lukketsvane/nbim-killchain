document.addEventListener('DOMContentLoaded', () => {
    console.log('Page loaded. Ready for interactive charts if needed.');
    
    // Future functionality: 
    // 1. Load JSON data dynamically to populate tables?
    // 2. Add simple filtering for the tables?
    // For now, the static content is fine as a starting point.
});
